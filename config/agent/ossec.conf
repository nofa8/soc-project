<ossec_config>
  <!-- DISABLE INTERNAL MANAGER INDEXER CONNECTOR -->
  <indexer>
    <enabled>no</enabled>
  </indexer>

  <!-- AGENT → MANAGER CONNECTION -->
  <client>
    <server>
      <address>wazuh-manager</address>
      <port>1514</port>
      <protocol>tcp</protocol>
    </server>
  </client>

  <!-- LOG COLLECTION -->

  <localfile>
    <location>/var/log/soc_logs/api/security.json</location>
    <log_format>json</log_format>
  </localfile>

  <localfile>
    <location>/var/log/soc_logs/nginx/access.log</location>
    <log_format>json</log_format>
  </localfile>

  <localfile>
    <location>/var/log/soc_logs/suricata/eve.json</location>
    <log_format>json</log_format>
  </localfile>

</ossec_config>
