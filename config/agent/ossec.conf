<ossec_config>
  <!-- AGENT → MANAGER CONNECTION -->
  <client>
    <server>
      <address>wazuh-manager</address>
      <port>1514</port>
      <protocol>tcp</protocol>
    </server>
  </client>

  <!-- LOG COLLECTION -->

  <localfile>
    <location>/var/log/soc_logs/api/security.json</location>
    <log_format>json</log_format>
  </localfile>

  <localfile>
    <location>/var/log/soc_logs/nginx/access.log</location>
    <log_format>json</log_format>
  </localfile>

  <localfile>
    <location>/var/log/soc_logs/suricata/eve.jsonl</location>
    <log_format>json</log_format>
  </localfile>

  <!-- Firewall Logs (exported from journald via systemd service) -->
  <localfile>
    <location>/var/log/firewall.log</location>
    <log_format>syslog</log_format>
  </localfile>

</ossec_config>
